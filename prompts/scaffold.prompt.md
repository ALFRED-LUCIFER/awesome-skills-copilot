---
description: 'Scaffold a new .NET 10 microservice (Dum-E @scaffold)'
agent: scaffold
---

Scaffold a new .NET 10 microservice from scratch. Follow the SampleService blueprint. After skeleton, delegate CRUD entities to Friday (@backend).

Service name: ${input:Enter the new service name (e.g. RackManagement)}
